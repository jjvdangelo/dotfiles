return {
    "jjvdangelo/canvas.nvim",
    lazy = false,
    priority = 1000,
    opts = { variant = "dark" },
    config = function(_, opts)
        -- require "canvas".setup(opts)
        -- vim.cmd.colorscheme "canvas"
    end
}
